services:
  - type: web
    name: advp-curator-demo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 advp_run_ui.py web --host 0.0.0.0 --port $PORT
